public class ArrayList {
    /**
     * Main methods in List Interface
     * +----------------------------------------------------------------------------------------------+
     * | E get (int index);              | Returns the elements at position index in this list object |
     * | E set (int index, E element)    | Replaces element at index                                  |
     * | int indexOf (Object element)    | Returns index of the first parameter                       |
     * | void add (int index, E element) | Insert element at index                                    |
     * | E remove (int index)            | Remove element at index                                    |
     * | int size()                      | Returns list size                                          |
     * | void clear()                    | Clears the list                                            |
     * | boolean isEmpty()               | Returns true if the list is empty                          |
     * +----------------------------------------------------------------------------------------------+
     *              
     *         when removing remember to i-- (see MarchJuly2021Ass3 Q1)     
     * 
     * 
     */
}
